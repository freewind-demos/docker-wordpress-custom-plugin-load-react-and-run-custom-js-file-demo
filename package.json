{
  "scripts": {
    "up": "docker-compose up -d",
    "postup": "open http://localhost:20080/wp-admin & open http://localhost:20081",
    "down": "docker-compose down",
    "build-plugin": "cd ./src && zip -r ../dist/plugin.zip ."
  }
}
